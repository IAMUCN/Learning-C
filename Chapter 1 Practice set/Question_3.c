#include<stdio.h>

int main(){
    float celsius,farenhit;
    printf("Enter the value of celsius: ");
    scanf("%f",&celsius);
    printf("The temperature in farenhit is : %f ", (celsius*1.8)+32);
    
return 0;
}