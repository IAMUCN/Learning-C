#include <stdio.h>
int main() {
    int a; 
    int sachin_is_good;
    // We an not use brackets and white spaces , no special characters other than _ ,
    // and we can not even  name a1 like java



    printf("%d",a);
   return 0;
}